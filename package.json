{
  "name": "beutech-vacation-timeline",
  "version": "1.0.0",
  "private": true,
  "description": "Staffbase Vacation Timeline Widget + Azure Functions Backend",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:widget": "npm run dev -w apps/widget",
    "dev:backend": "npm run dev -w apps/backend",
    "build:widget": "npm run build -w apps/widget",
    "build:backend": "npm run build -w apps/backend",
    "build": "npm run build:widget && npm run build:backend",
    "lint": "npm run lint -w apps/widget && npm run lint -w apps/backend",
    "lint:secrets": "secretlint \"**/*\"",
    "test": "npm run test -w apps/widget && npm run test -w apps/backend",
    "clean": "rm -rf apps/*/dist apps/*/node_modules node_modules"
  },
  "devDependencies": {
    "@secretlint/secretlint-rule-preset-recommend": "^8.0.0",
    "secretlint": "^8.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "Beutech",
  "license": "UNLICENSED"
}
